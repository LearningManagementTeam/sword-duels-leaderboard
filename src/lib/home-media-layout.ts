/** Shared width for home hero logo + photo carousel — mobile-first, scales up on larger screens. */
export const HOME_MEDIA_SHELL =
  "mx-auto w-full max-w-full sm:max-w-lg md:max-w-xl lg:max-w-2xl";

/** Carousel / photo frame — fixed 16:9, rounded, no border chrome. */
export const HOME_CAROUSEL_FRAME =
  "relative aspect-video w-full overflow-hidden rounded-2xl shadow-[0_12px_40px_rgb(0_0_0/0.4)] ring-1 ring-white/[0.06] sm:rounded-3xl";
